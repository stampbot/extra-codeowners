Extra CODEOWNERS has been installed as {{ include "extra-codeowners.fullname" . }}.

Verify the rollout:
  kubectl --namespace {{ .Release.Namespace }} rollout status deployment/{{ include "extra-codeowners.fullname" . }}

Run the chart test after the pod becomes ready:
  helm test {{ .Release.Name }} --namespace {{ .Release.Namespace }}

{{- if .Values.migrations.enabled }}
The pre-install/pre-upgrade migration Job completed before application pods
were changed. Preserve its logs with the release evidence before the Job TTL
expires, and verify the database backup separately before every upgrade.
{{- else }}
The chart migration Job is disabled. Verify that a separately controlled
migrator applied the exact target head before application startup.
{{- end }}

{{- if not .Values.existingSecret }}

WARNING: existingSecret is empty. The application requires GitHub App and
persistence configuration before its readiness probe can succeed.
{{- end }}
{{- if and .Values.migrations.enabled (not .Values.migrations.existingSecret) (not .Values.migrations.extraEnvFrom) (not .Values.migrations.extraEnv) }}

WARNING: no migration database configuration is set. The migration Job needs a
PostgreSQL URL from migration-only Secret or environment values. Do not attach
runtime GitHub App credentials to it.
{{- end }}
{{- if .Values.allowInsecureChanges }}

SECURITY WARNING: allowInsecureChanges is enabled. Application approvals may
satisfy changes to built-in security-sensitive policy, workflow, and
repository-local action paths. Organization guardrails still apply. Confirm
that this cluster-wide authority change is intentional before accepting any
Extra CODEOWNERS check.
{{- end }}
{{- if not .Values.ingress.enabled }}

Ingress is disabled. Forward the Service locally for diagnostics:
  kubectl --namespace {{ .Release.Namespace }} port-forward service/{{ include "extra-codeowners.fullname" . }} 8000:{{ .Values.service.port }}
{{- end }}
